/**
 * tRPC stub — backend removed for static prototype.
 * The real server is not needed; this keeps imports compiling.
 */
export const trpc = {
  Provider: ({ children }: { children: React.ReactNode }) => children,
} as any;

export function createTRPCClient() {
  return {} as any;
}
